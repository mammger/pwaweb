# pwaweb

最简单的 PWA 示例页面，包含：

- `manifest.webmanifest`
- `service worker` 离线缓存
- 基础页面与图标

## 本地运行

可使用任意静态服务器（需 `http://localhost` 或 `https`）：

```bash
python3 -m http.server 8080
```

打开 `http://localhost:8080`。
